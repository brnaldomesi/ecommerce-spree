= render partial: 'spree/variants/variants_wrapper', locals: { variants: @variants }
%br/
%br/
.container
  .text-center.pt-1.mb-4
    - if @variants.respond_to?(:total_pages)
      = paginate @variants
